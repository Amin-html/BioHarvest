services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: bioharvest
      POSTGRES_PASSWORD: bioharvest
      POSTGRES_DB: bioharvest
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: